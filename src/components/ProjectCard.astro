---
interface Props {
  href: string;
  title: string;
  summary: string;
  eyebrow?: string;
  status?: string;
  surface?: "solid" | "glass";
  tone?: "football" | "basketball" | "article" | "archive";
  tags?: string[];
}

const {
  href,
  title,
  summary,
  eyebrow,
  status,
  surface = "solid",
  tone = "football",
  tags = []
} = Astro.props;
---

<article class={`project-card reveal tone-${tone} surface-${surface}`}>
  <div class="card-top">
    <div class="card-copy">
      {eyebrow && <p class="card-eyebrow">{eyebrow}</p>}
      <h3><a href={href}>{title}</a></h3>
    </div>
    {status && <span class="status-pill">{status}</span>}
  </div>

  <p class="card-summary">{summary}</p>

  {tags.length > 0 && (
    <ul class="tag-row" aria-label={`${title} tags`}>
      {tags.map((tag) => <li>{tag}</li>)}
    </ul>
  )}

  <div class="card-actions">
    <a class="card-link" href={href}>Open</a>
    <slot name="actions" />
  </div>
</article>

<style>
  .project-card {
    display: grid;
    gap: 1.1rem;
    min-height: 100%;
    padding: 1.4rem;
    border-radius: var(--radius-lg);
    border: 1px solid var(--line);
    background:
      linear-gradient(180deg, rgba(255, 255, 255, 0.02), transparent 30%),
      linear-gradient(160deg, rgba(19, 30, 42, 0.96), rgba(13, 22, 31, 0.98));
    box-shadow: var(--shadow-card);
    position: relative;
    overflow: hidden;
  }

  .project-card::before {
    content: "";
    position: absolute;
    inset: 0 auto auto 0;
    width: 100%;
    height: 1px;
    background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.16), transparent);
  }

  .project-card::after {
    content: "";
    position: absolute;
    inset: -4rem -3rem auto auto;
    width: 10rem;
    height: 10rem;
    border-radius: 999px;
    opacity: 0.18;
    filter: blur(16px);
    background: var(--tone, var(--accent));
  }

  .project-card.surface-glass {
    border: 1px solid rgba(255, 255, 255, 0.28);
    background: none !important;
    background-color: transparent !important;
    background-image: none !important;
    box-shadow:
      inset 0 1px 0 rgba(255, 255, 255, 0.22),
      inset 0 -1px 0 rgba(255, 255, 255, 0.03),
      0 8px 18px rgba(0, 0, 0, 0.06);
    transition:
      transform 180ms ease,
      box-shadow 180ms ease,
      border-color 180ms ease;
  }

  .project-card.surface-glass::before {
    content: "";
    position: absolute;
    inset: 0;
    width: auto;
    height: auto;
    border-radius: inherit;
    padding: 1px;
    background: linear-gradient(
      135deg,
      rgba(125, 211, 252, 0.55),
      rgba(94, 234, 212, 0.2) 45%,
      rgba(255, 255, 255, 0.06) 100%
    );
    -webkit-mask:
      linear-gradient(#fff 0 0) content-box,
      linear-gradient(#fff 0 0);
    -webkit-mask-composite: xor;
    mask:
      linear-gradient(#fff 0 0) content-box,
      linear-gradient(#fff 0 0);
    mask-composite: exclude;
    opacity: 0;
    transition: opacity 180ms ease;
    pointer-events: none;
  }

  .project-card.surface-glass::after {
    display: none;
  }

  .project-card.surface-glass:hover {
    transform: translateY(-4px);
    box-shadow:
      inset 0 1px 0 rgba(255, 255, 255, 0.3),
      inset 0 -1px 0 rgba(255, 255, 255, 0.04),
      0 14px 28px rgba(0, 0, 0, 0.1);
    border-color: rgba(255, 255, 255, 0.34);
  }

  .project-card.surface-glass:hover::before {
    opacity: 1;
  }

  .card-top {
    display: flex;
    align-items: flex-start;
    justify-content: space-between;
    gap: 1rem;
  }

  .card-eyebrow {
    margin: 0 0 0.45rem;
    font-size: 0.74rem;
    letter-spacing: 0.1em;
    text-transform: uppercase;
    color: var(--muted);
  }

  h3 {
    margin: 0;
    font-family: var(--font-display);
    font-size: 1.45rem;
    line-height: 1.02;
    letter-spacing: -0.03em;
  }

  h3 a {
    color: var(--text);
    text-decoration: none;
  }

  .card-summary {
    margin: 0;
    color: var(--muted);
    line-height: 1.75;
  }

  .tag-row {
    display: flex;
    flex-wrap: wrap;
    gap: 0.5rem;
    margin: 0;
    padding: 0;
    list-style: none;
  }

  .tag-row li,
  .status-pill {
    display: inline-flex;
    align-items: center;
    min-height: 1.9rem;
    padding: 0.15rem 0.62rem;
    border-radius: 999px;
    border: 1px solid rgba(255, 255, 255, 0.09);
    background: rgba(255, 255, 255, 0.04);
    color: var(--text-soft);
    font-size: 0.75rem;
    font-weight: 600;
    letter-spacing: 0.04em;
    text-transform: uppercase;
  }

  .card-actions {
    display: flex;
    gap: 0.9rem;
    flex-wrap: wrap;
  }

  .card-link {
    color: var(--text);
    text-decoration: none;
    font-weight: 600;
  }

  .tone-football {
    --tone: var(--accent-football);
  }

  .tone-basketball {
    --tone: var(--accent-basketball);
  }

  .tone-article {
    --tone: var(--accent-method);
  }

  .tone-archive {
    --tone: var(--accent-archive);
  }
</style>
